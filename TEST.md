# Rust モジュール管理 & テスト ガイド

Rustでは、ファイル構造とモジュール階層が密接に連動しています。「ファイルがあるだけ」では認識されず、必ず親モジュールからの明示的な宣言が必要です。

## 1. モジュール構成の基本ルール

### 階層構造の作り方

Rust 2018エディション以降、主に2つの書き方がありますが、ディレクトリごとに整理する場合は**`mod.rs`** を使う方法が分かりやすいです。

- **宣言 (`mod`)**: そのファイル/ディレクトリをモジュールとして取り込む。
- **公開 (`pub`)**: 外部（他のモジュールや `main.rs`）からアクセス可能にする。

#### 例：ディレクトリ構成

```text
src/
├── main.rs         (1) 全ての親
└── parser/         (2) ディレクトリをモジュール化
    ├── mod.rs      (3) parserの入り口
    └── ast.rs      (4) 具体的な処理

```

#### 実装コード

```rust
// (3) src/parser/mod.rs
// 同じ階層の ast.rs を読み込み、外部に公開する
pub mod ast;

// (1) src/main.rs
// parserディレクトリを読み込む
mod parser;

fn main() {
    // 使うときはパスを指定する
    let node = parser::ast::Node::new();
}

```

---

## 2. モジュールの外部利用（パスの指定）

他のファイルにある機能を使いたい場合、3つの指定方法があります。

| 種類           | 書き方                                | 説明                                                            |
| -------------- | ------------------------------------- | --------------------------------------------------------------- |
| **絶対パス**   | `crate::parser::ast`                  | `src/main.rs`（crateルート）から辿る。一番確実。                |
| **相対パス**   | `super::state`                        | 一つ上の親階層から辿る。テストコードでよく使う。                |
| **インポート** | `use crate::env::state::Environment;` | 長いパスを省略して、以降 `Environment` だけで使えるようにする。 |

---

## 3. テストの書き方

Rustでは、同じファイル内にテストを書く「単位テスト」と、`tests/` ディレクトリに書く「結合テスト」があります。

### 単位テスト (Unit Tests)

各ファイル（`state.rs` など）の末尾に直接書きます。非公開（`pub` でない）関数もテストできるのがメリットです。

```rust
// --- src/env/state.rs ---
pub struct Environment { ... }

#[cfg(test)] // テスト実行時のみコンパイルされる
mod tests {
    use super::*; // 親(state.rs)の定義をすべてインポート

    #[test]
    fn test_my_feature() {
        let env = Environment::new();
        assert_eq!(env.count(), 0);
    }
}

```

### 結合テスト (Integration Tests)

プロジェクトのルートに `tests/` ディレクトリを作り、ライブラリとして外部から呼ぶ形式でテストします。

```text
.
├── src/
└── tests/
    └── shell_tests.rs  # 公開APIのみをテストする

```

---

## 4. テストの実行コマンド

`cargo test` は強力なフィルタリング機能を備えています。

- **プロジェクト全体のテストを実行**

```bash
cargo test

```

- **特定のモジュールだけ実行**（ディレクトリ名やファイル名で指定）

```bash
cargo test env       # envディレクトリ以下の全テスト
cargo test env::state # env/state.rs のテストだけ

```

- **特定の名前のテスト関数だけ実行**

```bash
cargo test test_loading_env

```

- **print文の内容を表示する**
  （通常、成功したテストのログは隠されます）

```bash
cargo test -- --nocapture

```

---

## 5. よくあるトラブルシューティング

1. **"module not found" と言われる**

- 親の `mod.rs` または `main.rs` で `mod 子モジュール名;` を書き忘れていませんか？

2. **テストが0件になる**

- `#[test]` 属性を関数につけていますか？
- そのファイルが `main.rs` から `mod` チェーンで繋がっていますか？

3. **非公開の構造体が見えない**

- テストモジュール内で `use super::*;` を忘れていませんか？

---
